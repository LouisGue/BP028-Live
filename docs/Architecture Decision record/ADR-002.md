# ADR-002 : Méthode de bootstrap du système de base
## Contexte
La première étape de construction de l'image est l'obtention d'un système de base Debian minimal. Cette décision conditionne l'ensemble du projet : tout ce qui sera présent dans le chroot à ce stade constitue la surface initiale à durcir. Elle doit être documentée car elle impacte directement la conformité avec R56 (installation minimale) du référentiel BP-028.

## Options évaluées
### debootstrap --variant=minbase
Variante la plus réduite de debootstrap. Installe uniquement les paquets de priorité `required` et les dépendances strictes de `apt`. Ne contient aucun paquet `important` — pas de `systemd`, pas de shell interactif au-delà de `dash`, pas d'éditeur, pas de documentation.

### debootstrap (variante par défaut)
Installe les paquets `required` et `important`. Inclut des paquets non strictement nécessaires au fonctionnement d'un système de base (cron, logrotate, nano, etc.), ce qui va à l'encontre de l'approche minimaliste.

### mmdebstrap
Alternative plus moderne à debootstrap, plus rapide et plus flexible. Cependant, non disponible dans les dépôts Nobara/Fedora sans ajout de sources supplémentaires, et l'avantage fonctionnel ne justifie pas la complexité ajoutée pour ce projet.

## Critères de choix
- **Minimalité** : le système de base doit contenir le strict nécessaire, chaque paquet supplémentaire devant être ajouté explicitement et justifié
- **Disponibilité de l'outil** : l'outil doit être installable simplement sur Nobara 43 (système hôte)
- **Cohérence avec BP-028 R56** : l'installation minimale est un prérequis au durcissement couche par couche
- **Reproductibilité** : la commande doit être simple, documentée et rejouable

## Décisions techniques détaillées
### Variante minbase
`--variant=minbase` est retenu car il produit le système le plus réduit possible. Tout ajout ultérieur sera explicite et traçable dans les scripts de construction.

### Transport HTTP au bootstrap
Le miroir est accédé en HTTP (`http://deb.debian.org/debian`). Ce choix est délibéré : `debootstrap` vérifie l'intégrité des paquets via les signatures GPG du dépôt, pas via TLS. Le paquet `ca-certificates` n'est donc pas nécessaire au bootstrap. Il sera installé ultérieurement dans le chroot lors du passage des sources en HTTPS.

### Absence de --include
Aucun paquet supplémentaire n'est injecté au bootstrap via `--include=`. Le chroot initial ne contient que ce que `minbase` fournit. Les paquets nécessaires (ca-certificates, gnupg, etc.) seront ajoutés dans une étape séparée, documentée et justifiée.

## Commande retenue
```bash
debootstrap --variant=minbase trixie ./chroot http://deb.debian.org/debian
```

## Conséquences
- Le chroot initial est le plus petit possible (~200 Mo)
- Chaque paquet ajouté par la suite devra être justifié
- Le passage en HTTPS et l'ajout de clés GPG supplémentaires font partie de l'étape suivante
- La traçabilité complète de la surface installée est assurée dès le départ
